import React, { useContext, useEffect, useState } from "react";
import { Link, useSearchParams } from "react-router-dom";
import { LJKAContext } from "../context/LJKAContext";

const SahyogDonations = () => { const { backendUrl } = useContext(LJKAContext); const [params, setParams] = useSearchParams(); const [rows, setRows] = useState([]); const [meta, setMeta] = useState(null); const [searchInput, setSearchInput] = useState(params.get("search") || ""); const [error, setError] = useState(""); const page = Number(params.get("page") || 1); const query = params.toString(); const load = () => { setError(""); fetch(`${backendUrl}/api/public/sahyog/donations?${query}`).then((r) => r.ok ? r.json() : Promise.reject()).then((r) => { setRows(r.donations); setMeta(r.pagination); }).catch(() => setError("Unable to load donations.")); }; useEffect(load, [backendUrl, query]); const search = (e) => { e.preventDefault(); const next = new URLSearchParams(params); next.set("page", "1"); searchInput.trim() ? next.set("search", searchInput.trim()) : next.delete("search"); setParams(next); }; return <main className="min-h-screen bg-gray-50"><section className="mx-auto max-w-[1400px] px-4 py-8 sm:px-6"><Link className="text-sm font-semibold text-[var(--ljka-primary)]" to="/sahyog-list">← Sahyog</Link><h1 className="mt-4 text-3xl font-bold text-gray-900">Recent Donations</h1><p className="mt-1 text-sm text-gray-500">Latest verified donations first. Anonymous donors remain anonymous.</p><form onSubmit={search} className="mt-5 flex max-w-lg gap-2"><input value={searchInput} onChange={(e) => setSearchInput(e.target.value)} placeholder="Member ID, donor or late member" className="min-w-0 flex-1 rounded-lg border bg-white p-3" /><button className="rounded-lg bg-[var(--ljka-primary)] px-4 text-white">Search</button></form>{error ? <div className="mt-6 rounded-xl bg-white p-6 text-center"><p>{error}</p><button onClick={load} className="mt-3 font-semibold text-[var(--ljka-primary)]">Retry</button></div> : <div className="mt-6 overflow-x-auto rounded-xl border border-gray-200 bg-white shadow-sm"><table className="min-w-full text-left text-sm"><thead className="bg-gray-50 text-gray-500"><tr>{["S.No", "Member ID", "Member name", "District", "Block / Tehsil", "Late user", "Donated at"].map((x) => <th key={x} className="p-4 font-medium">{x}</th>)}</tr></thead><tbody>{rows.map((row, index) => <tr key={row._id} className="border-t"><td className="p-4">{(page - 1) * 20 + index + 1}</td>{row.donor ? <><td className="p-4">{row.donor.memberId}</td><td className="p-4">{row.donor.fullName}</td><td className="p-4">{row.donor.district || "—"}</td><td className="p-4">{row.donor.tehsil || "—"}</td></> : <td colSpan="4" className="p-4 font-medium">Anonymous</td>}<td className="p-4">{row.lateMember.fullName}</td><td className="p-4">{new Date(row.donatedAt).toLocaleString("en-IN")}</td></tr>)}</tbody></table>{!rows.length && <p className="p-8 text-center text-gray-500">No donations yet.</p>}</div>}<Pager page={page} pages={meta?.pages || 0} setPage={(value) => { const next = new URLSearchParams(params); next.set("page", value); setParams(next); }} /></section></main>; };
const Pager = ({ page, pages, setPage }) => pages > 1 ? <div className="mt-5 flex justify-center gap-3"><button disabled={page <= 1} onClick={() => setPage(page - 1)} className="rounded-lg border bg-white px-4 py-2 disabled:opacity-40">Previous</button><span className="px-3 py-2 text-sm">Page {page} of {pages}</span><button disabled={page >= pages} onClick={() => setPage(page + 1)} className="rounded-lg border bg-white px-4 py-2 disabled:opacity-40">Next</button></div> : null;
export default SahyogDonations;
